interface:
  display_name: "AI News"
  short_description: "Daily/weekly AI news to Feishu with source links"
  default_prompt: "Use $ai-news to run the AI industry news workflow. Archive to Base, build card with source links, publish directly to group. No admin approval."
